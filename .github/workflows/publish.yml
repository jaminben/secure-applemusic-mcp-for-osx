name: publish

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build:
    name: Build sdist + wheel
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: Install uv
        uses: astral-sh/setup-uv@caf0cab7a618c569241d31dcd442f54681755d39  # v3.2.4
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.12

      - name: Build distributions
        run: uv build

      - name: Upload build artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4.6.2
        with:
          name: dist
          path: dist/

  publish:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    # Trusted Publishing — no API token needed in repo secrets.
    # Configure once at https://pypi.org/manage/project/applemusic-mcp/settings/publishing/
    # with publisher: GitHub, owner: epheterson, repo: applemusic-mcp,
    # workflow: publish.yml, environment: pypi
    environment:
      name: pypi
      url: https://pypi.org/project/applemusic-mcp/
    permissions:
      id-token: write
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4.3.0
        with:
          name: dist
          path: dist/

      - name: Publish to PyPI
        # Intentionally tracks the maintained release/v1 branch (NOT a pinned
        # SHA): this is PyPA's first-party publish action using OIDC trusted
        # publishing (no long-lived secret), and release/v1 is the channel PyPA
        # ships security patches on. Pinning it would opt out of those patches
        # for little gain and add manual-bump toil. The other actions stay
        # SHA-pinned.
        uses: pypa/gh-action-pypi-publish@release/v1
