# Secrets-scan allowlist. Each entry suppresses a known false positive.
# Real secrets must NEVER be added.

# Test fixtures — deliberately fake credentials that exercise developer-token
# generation / JWT signing. Not live secrets (TEST_TEAM_ID / test_signature /
# a throwaway key).
tests/conftest.py:BEGIN PRIVATE KEY
tests/conftest.py:test_signature
